# 快速安装指南

## 🚀 5 分钟安装 Chrome 插件

### 步骤 1: 准备插件文件

确保你已经拥有完整的插件文件夹，包含以下文件：
```
youhouplugin/
├── manifest.json      (必需)
├── content.js         (必需)
├── background.js      (必需)
└── README.md          (可选)
```

### 步骤 2: 打开 Chrome 扩展管理页面

1. 在 Chrome 浏览器地址栏输入：`chrome://extensions/`
2. 按回车键打开扩展管理页面

### 步骤 3: 开启开发者模式

在页面右上角找到"开发者模式"开关，将其打开

### 步骤 4: 加载插件

1. 点击"加载已解压的扩展程序"按钮
2. 选择 `youhouplugin` 文件夹（整个文件夹）
3. 点击"选择文件夹"

### 步骤 5: 验证安装

✅ 如果安装成功，你会看到：
- 扩展列表中出现"抖音广告自动跳过"扩展
- 扩展图标为灰色拼图形状（因为没有自定义图标）
- 状态显示为"已启用"

### 步骤 6: 使用插件

1. 打开新标签页
2. 访问：https://www.douyin.com/?recommend=1
3. 按 F12 打开开发者工具
4. 切换到 Console（控制台）标签
5. 当刷到广告时，你应该能看到类似输出：
   ```
   [抖音广告跳过] 插件初始化
   [抖音广告跳过] 开始检测广告
   [抖音广告跳过] 检测到广告关键词：广告
   [抖音广告跳过] 执行划走动作
   ```

## 🔧 常见问题

### Q1: 加载时提示"清单文件缺失或不可读"
**解决**: 确保选择的是包含 manifest.json 的文件夹

### Q2: 扩展无法运行
**解决**: 
1. 检查是否在抖音网站 (`https://www.douyin.com/*`)
2. 查看控制台是否有错误信息
3. 刷新抖音网页

### Q3: 没有看到广告跳过效果
**解决**:
1. 打开控制台查看是否有日志输出
2. 可能需要等待一段时间才会遇到广告
3. 检查广告关键词是否匹配

### Q4: 想要自定义图标
**解决**: 
参考 `icons/README.md` 生成 PNG 图标，然后在 manifest.json 中恢复图标配置

## 📝 调试技巧

### 查看 Content Script 日志
1. 在抖音页面按 F12
2. 打开 Console（控制台）
3. 筛选 "[抖音广告跳过]" 相关的日志

### 查看 Background Script 日志
1. 访问 `chrome://extensions/`
2. 找到"抖音广告自动跳过"扩展
3. 点击"检查视图 - Service Worker"
4. 在新窗口中查看控制台日志

### 临时禁用/启用插件
在扩展管理页面，使用扩展卡片上的开关按钮

## 🎯 配置调整

如果需要调整广告检测灵敏度，编辑 `content.js`:

```javascript
const CONFIG = {
  checkInterval: 500,     // 更频繁检测（默认 1000ms）
  swipeDelay: 300,        // 更快划走（默认 500ms）
  adKeywords: [           // 添加更多关键词
    '广告',
    '你的关键词'
  ]
};
```

修改后需要在扩展管理页面点击"刷新"按钮重新加载扩展。

## 💡 提示

- 插件只在抖音网站生效，不影响其他网站
- 可以通过点击扩展图标来临时启用/禁用
- 控制台日志会显示跳过的广告数量
- 建议使用键盘方向键手动测试抖音的操作方式

祝你使用愉快！🎉
